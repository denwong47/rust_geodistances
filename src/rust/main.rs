/// This module is not in use.
/// 
/// This repo is built as a lib; `cargo run` has no meaning.
fn main() -> Result<(), ()> {
    println!("rust_geodistances cannot be run via `cargo run`;");
    println!("Use `maturin build` and use in Python instead.");
    return Ok(());
}
